# Core
sounddevice>=0.5.0
numpy>=1.26.0
python-dotenv>=1.0.0
groq>=0.9.0
requests>=2.28.0
PyQt5>=5.15.9

# Better VAD (ML-based speech detection, optional but recommended)
webrtcvad>=2.0.10

# Linux: evdev for global hotkeys (via raw /dev/input)
# Requires: sudo usermod -aG input $USER
evdev>=1.6.0; sys_platform == 'linux'

# Windows / macOS: pynput for global hotkeys
pynput>=1.7.6; sys_platform == 'win32' or sys_platform == 'darwin'

# macOS: PyObjC for screen capture exclusion (optional)
# pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'

# Build (optional)
pyinstaller>=6.0
