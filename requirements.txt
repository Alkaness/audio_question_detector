sounddevice>=0.4.6
numpy>=1.24.0
python-dotenv>=1.0.0
groq>=0.4.0
pyttsx3>=2.90
PyQt5>=5.15.0

# Linux: evdev for global hotkeys (raw /dev/input)
# Requires: sudo usermod -aG input $USER
evdev>=1.6.0; sys_platform == 'linux'

# Windows & macOS: pynput for global hotkeys
pynput>=1.7.0; sys_platform == 'win32'
pynput>=1.7.0; sys_platform == 'darwin'

# macOS: PyObjC for screen capture exclusion (NSWindow.sharingType)
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'
