# Core
sounddevice>=0.5.0
numpy>=1.26.0
python-dotenv>=1.0.0
groq>=0.9.0
requests>=2.28.0
PyQt5>=5.15.9

# OpenAI provider (optional — for GPT-4o answers and vision)
openai>=1.0.0

# Screen capture (for coding interview screenshot analysis)
mss>=9.0.0
Pillow>=10.0.0

# Resume parsing (optional — for personalized answers)
PyPDF2>=3.0.0
python-docx>=1.0.0

# Better VAD (ML-based speech detection, optional but recommended)
webrtcvad>=2.0.10

# Linux: evdev for global hotkeys (via raw /dev/input)
# Requires: sudo usermod -aG input $USER
evdev>=1.6.0; sys_platform == 'linux'

# Windows / macOS: pynput for global hotkeys
pynput>=1.7.6; sys_platform == 'win32' or sys_platform == 'darwin'

# macOS: PyObjC for screen capture exclusion (optional)
# pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'

# Process name masking (optional — for enhanced stealth)
setproctitle>=1.3.0

# Build (optional)
pyinstaller>=6.0

