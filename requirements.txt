sounddevice>=0.4.6
numpy>=1.24.0
python-dotenv>=1.0.0
groq>=0.4.0
pyttsx3>=2.90
PyQt5>=5.15.0

# Linux: evdev for global hotkeys (via raw /dev/input)
# Install: pip install evdev
# Requires: sudo usermod -aG input $USER
evdev>=1.6.0; sys_platform == 'linux'

# Windows: pynput for global hotkeys
pynput>=1.7.0; sys_platform == 'win32'
